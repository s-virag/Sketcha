export enum Action {
    None = "none",
    Move = "move",
    Draw = "draw",
    Resize = "resize",
    Pan = "pan"
}

export enum Tool {
    Selection = "selection",
    Line = "line",
    Rectangle = "rectangle",
}
