/*

class FreehandElement implements Element {
  id: number;
  type: ElementType;
  color: string;
  points: { x: number; y: number }[];

  constructor(
    id: number,
    type: ElementType,
    color: string,
    points: { x: number; y: number }[]
  ) {
    this.id = id;
    this.type = type;
    this.color = color;
    this.points = points;
  }
}*/